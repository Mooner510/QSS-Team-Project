using System;
using UnityEngine;

namespace Entity
{
    public class Shooter : MonoBehaviour
    {
        private void Update()
        {
            if (Input.GetKey(KeyCode.Space))
            {
                //TODO: We Have to Add Shooting Code
            }
        }
    }
}